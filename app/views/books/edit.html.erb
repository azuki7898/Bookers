<h1>Editing book</h1>
<%= form_with model:@book, url:"/books/#{@book.id}", local:true do |f| %>
  <p>Title</p>
  <%= f.text_field :title %>
  <p>Body</p>
  <%= f.text_area :body %>
  <%= f.submit 'Update book' %>
<% end %>

<%= link_to 'Show', show_book_path %>
<%= link_to 'Back', book_path %>
